#include <stdio.h>
#include <string.h>
int main()
{
    char s[4] = "";
    int i,a[3];
    for(i=0;i<3;i++)
    {
        scanf("%d",&a[i]);
    }
    fflush(stdin);
    gets(s);
    if(!strcmp(s,"ABC"))
    {
        
    }
}
